import colors from "picocolors"

// ----------------------------------------------------

export type CreateCertificatesOptions = {

}

export const create_certificates = async (certificates_folder_path: string, options?: CreateCertificatesOptions) => {
  console.log(`create ${colors.green('certificates')}...`)

  console.log(colors.red('TODO: create certificates'))

  console.log(`${colors.green('certificates')} created`)
  console.log('')
}